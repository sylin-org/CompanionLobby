[package]
name = "adapter-service-tangent"
version = "0.1.0"
edition = "2021"

[dependencies]
companion-core = { path = "../../../core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
ureq = { version = "2", features = ["json"] }

urlencoding = "2"
